spring.application.name=rdpk
server.port=8080

# CPF validation service URL (our own service)
cpf.validation.url=http://localhost:8080/api/cpf-validation

# CPF Validation Configuration - Default to lenient for development
cpf.validation.lenient=true

